# Chatbot (branch `tk`)

Standalone hostel assistant from `origin/tk` (`widget-preview.html`).

## In Trinity Engine

Mounted by `frontend/src/components/TkChatbot.jsx` (replaces the old FAQ `FloatingAIAssistant`).

Assets:

- `frontend/public/tk-chatbot/widget.css`
- `frontend/public/tk-chatbot/markup.html`
- `frontend/public/tk-chatbot/widget.js`
- Preview: `frontend/widget-preview.html`

## Standalone preview

Open `frontend/widget-preview.html` in a browser, or visit the dashboard after login — launcher is bottom-right.
