# VITE_GUARDIAN_API_URL=http://127.0.0.1:8000
# Warden credentials are entered at /login — never bake passwords into the SPA.
# Server-side defaults live in the Python process env (see root .env.example).
